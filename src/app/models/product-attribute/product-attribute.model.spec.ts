import { ProductAttribute } from './product-attribute.model';

describe('ProductAttribute', () => {
  it('should create an instance', () => {
    expect(new ProductAttribute()).toBeTruthy();
  });
});
